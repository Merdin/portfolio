@page
@{
    ViewBag.Title = "Studieroutes";
}
<div class="flex flex-col">
    <div class="-my-2 overflow-x-auto sm:-mx-6 lg:-mx-8">
        <div class="py-2 align-middle inline-block min-w-full sm:px-6 lg:px-8">
            <a asp-controller="StudyPlan" asp-action="Create" class="hover:text-green-300">Studieroute aanmaken</a>
            <input type="text" id="searchInput" onkeyup="searchBar()" placeholder="Zoek naar studieroutes.."/>

            <div class="shadow overflow-hidden border-b border-gray-200 sm:rounded-lg">
                <table id="searchTable" class="min-w-full divide-y divide-gray-200">
                    <thead class="bg-gray-50">
                    <tr>
                        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                            Id
                        </th>
                        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                            Student Name
                        </th>
                        <th scope="col" class="px-6 py-3 text-left text-xs font-medium text-gray-500 uppercase tracking-wider">
                            Status
                        </th>
                        <th scope="col" class="relative px-6 py-3">
                            <span class="sr-only">Edit</span>
                        </th>
                    </tr>
                    </thead>
                    <tbody class="bg-white divide-y divide-gray-200">
                    @for (int i = 0; i < ViewBag.StudyPlans.Count; i++)
                    {
                        <tr>
                            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                                <span>@ViewBag.StudyPlans[i].User.Id</span>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm font-medium text-gray-900">
                                <span>@ViewBag.StudyPlans[i].User.Email</span>
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-sm text-gray-500">
                                @ViewBag.StudyPlans[i].Status
                            </td>
                            <td class="px-6 py-4 whitespace-nowrap text-right text-sm font-medium">
                                <a href="StudyPlan/Edit/@ViewBag.StudyPlans[i].Id" class="text-indigo-600 hover:text-indigo-900">Edit</a>
                            </td>
                        </tr>
                    }
                    </tbody>
                </table>
            </div>
        </div>
    </div>
</div>

